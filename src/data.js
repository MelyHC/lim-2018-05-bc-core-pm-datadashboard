window.computeUsersStats = (users, progress, courses) => {
 let user= console.log(users); 
  return user
}
window.sortUsers = (users,orderBy, orderDirection) => {

}
window.filterUsers = (users, search) => {

}
window.processCohortData = (options) => {
  
}

